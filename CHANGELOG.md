# Changelog

All notable changes to this project will be documented in this file.

## [1.1.0] - 2026-08-04

### UI 布局系统重构

#### 固定头部 + 独立滚动架构

- 实现全局「固定头部 + 内容区独立滚动」的分层布局架构
- 历史记录页面：主 Tab 导航固定，统计概览固定，检测历史头部固定，仅记录列表滚动
- 历史对比页面：报告标题和汇总卡片固定，仅下方详情内容滚动
- 系统配置页面：配置管理头部固定，下方 AI 配置与插件管理独立滚动
- 项目检测页面：操作区固定，检测结果卡片内统计卡片固定，仅结果列表滚动

#### AI 优化建议面板独立化

- AI 建议从检测结果过滤器中独立出来，改为右侧独立面板
- 采用左右分栏布局：左侧检测结果（flex: 2），右侧 AI 建议（flex: 1）
- AI 面板支持三种状态：空状态（引导点击）、流式加载中（自动滚动）、已完成
- 流式输出时自动滚动到底部，无需手动操作
- 响应式适配：小屏幕（≤768px）自动切换为上下堆叠布局

#### 项目选择与操作区优化

- 合并项目路径选择与当前项目显示为统一组件
- 未选择时显示「选择项目路径」按钮，已选择后显示项目名称+路径
- 按钮尺寸统一调整，操作区布局更紧凑合理

#### 历史记录体验优化

- 修复历史记录项删除按钮与日期显示重叠问题
- 对比结果卡片布局优化，支持按变更类型筛选（新增/删除/修改）
- 统计概览卡片支持点击切换筛选

### 配置持久化修复

#### 问题根因

- 配置保存后 IndexedDB 中存储正确，但应用重启后主进程插件状态未同步
- 主进程插件数组硬编码 `enabled: true`，仅在访问配置页时才通过 IPC 同步

#### 修复方案

- 新增 `sync-plugin-config` IPC 通道，支持批量同步插件配置
- 应用启动时 `loadConfig` 完成后立即调用同步接口
- 配置保存时同步调用，确保配置立即生效
- 形成「保存 → 持久化 → 主进程同步」的完整链路

### 新增检测插件

#### ESLint 配置检测插件

- 检测项目是否存在 ESLint 配置文件
- 检查 ESLint 配置的完整性和规范性

#### Vite 配置检测插件

- 检测项目是否存在 Vite 配置文件
- 验证 Vite 构建配置的合理性

### 新增功能

#### 对比结果详情查看

- 支持点击历史记录中的「与上次对比」按钮
- 高亮显示新增、删除、修改的问题项
- 汇总卡片展示各类变更数量统计

#### 报告导出增强

- Markdown 格式报告包含统计概览
- AI 优化建议独立章节
- 按问题等级分组展示

### 改进

#### 滚动体验

- 所有滚动区域添加自定义滚动条样式
- 流式输出自动跟随滚动
- 空状态下内容区自动填充容器

#### 响应式设计

- 桌面端（≥1024px）：完整左右分栏布局
- 平板端（≤768px）：AI 面板切换为下方堆叠
- 移动端（≤480px）：侧边栏底部固定，内容区自适应

#### 交互细节

- 按钮悬停动画优化
- 卡片阴影层次调整
- 状态徽章视觉统一
- 加载状态组件优化

### 重构

#### 组件架构

- `ScanPage`：左右分栏布局，AI 面板独立
- `HistoryPage`：固定头部 + 滚动列表结构
- `CompareResult`：固定汇总卡片 + 滚动详情
- `ConfigPage`：固定头部 + 滚动配置区
- `Tabs`：支持固定 header + flex content 滚动
- `ResultItem`：独立组件，支持级别样式

#### 样式系统

- 新增 `.scan-main-content`、`.ai-suggestions-area` 等分栏样式
- 新增 `.scan-results-scroll`、`.ai-panel-scroll` 滚动容器样式
- 新增 `.scan-stats-bar`、`.stat-item` 统计卡片样式
- 清理废弃的 `.ai-advice-card` 样式
- CSS 变量体系扩展

#### IPC 通信

- 新增 `sync-plugin-config` 通道（批量同步插件配置）
- 优化 `toggle-plugin` 通道（单插件状态切换）
- AI 流式输出通道优化

### 修复

- 修复配置页卡片内容被截断问题（`.card-body` 重复定义冲突）
- 修复配置页粘性定位与 overflow 冲突问题
- 修复历史记录删除按钮与日期文字重叠问题
- 修复流式输出滚动条不跟随问题
- 修复空状态下内容区域未填充容器问题

### 测试结果

- ✅ TypeScript 类型检查通过
- ✅ 所有页面布局在不同分辨率下正常显示
- ✅ 配置持久化在应用重启后正确生效
- ✅ AI 流式输出自动滚动正常
- ✅ 左右分栏布局在各屏幕尺寸下自适应

## [1.0.0] - 2026-07-28

### 性能优化（核心改进）

#### 后台工作线程

- 新增 `scanner-worker.ts`，将扫描任务放到独立线程执行
- 使用 Node.js `worker_threads` API，避免阻塞主线程
- 大型项目扫描时界面保持流畅响应

#### 并行扫描架构

- 重构 `scanner.ts`，使用 `Promise.all` 并行执行所有插件
- 扫描速度大幅提升：总时间 ≈ 最慢插件时间（而非所有插件之和）
- 充分利用多核 CPU 性能

#### 构建配置优化

- 配置 worker_threads 为外部依赖
- 将 scanner-worker 作为独立入口点构建

### 新增

#### 功能特性

- **历史记录页面**：完整展示所有检测历史记录，支持单条删除和清空全部操作，统计概览卡片展示累计数据
- **信息分类标签化展示**：检测结果分为5个标签页（全部/高危问题/警告提示/信息提示/AI优化建议），每个标签页带有数量徽章
- **Tabs组件**：通用标签页组件，支持图标、徽章、平滑切换动画

#### UI组件库

- `Button`：按钮组件，支持3种变体（primary/secondary/accent）和3种尺寸（sm/md/lg）
- `Card`：卡片容器组件，支持标题和头部右侧内容
- `Badge`：标签徽章组件，支持4种语义变体（error/warning/info/success）
- `Input`：输入框组件，支持label、type、placeholder等属性
- `ToggleSwitch`：开关切换组件
- `Loading`：加载状态组件
- `EmptyState`：空状态提示组件
- `ResultItem`：扫描结果项组件，根据级别展示不同样式
- `HistoryItem`：历史记录项组件
- `Sidebar`：侧边栏导航组件，支持悬停展开动画

#### 目录结构

- `src/renderer/src/components/`：UI组件目录
- `src/renderer/src/types/`：类型定义目录
- `src/renderer/src/utils/`：工具函数目录（config-store、db）
- `src/main/workers/`：后台工作线程

### 改进

#### 设计系统

- 建立完整的CSS变量体系：色彩体系（主色调、辅助色、中性色、语义色）
- 排版规范：8级字号系统、4级字重、3级行高
- 间距系统：12级间距变量（4px - 80px）
- 圆角系统：6级圆角变量（4px - 9999px）
- 阴影系统：5级阴影变量
- 过渡动画：3级过渡时长

#### 界面布局

- 现代化侧边栏布局：64px窄模式，悬停展开至180px
- 卡片式内容布局，信息层次分明
- 响应式设计：适配桌面端（≥768px）、平板端（≤768px）、移动端（≤480px）

#### 交互体验

- 平滑过渡动画：hover效果、展开动画、加载状态
- 完善的加载状态和空状态提示
- 按钮尺寸优化，提升可点击性
- AI分析完成后自动切换到AI标签页
- 历史记录点击快速加载到检测页面

### 重构

#### 代码结构

- 将ScanPage从App.tsx中提取为独立组件
- 移除所有内联样式，统一使用CSS类
- 组件化拆分，单一职责原则
- 代码结构清晰，便于维护和扩展

#### 样式优化

- 重写base.css，建立设计系统基础变量
- 重写main.css，统一组件样式和布局
- 优化滚动条样式
- 添加动画类（fadeIn、slideIn、pulse、spin）

### 技术栈更新

- Electron 39 + React 19 + TypeScript
- electron-vite 5
- ESLint 9 + Prettier 3

### 测试结果

- ✅ TypeScript类型检查通过
- ✅ 生产构建成功
- ✅ 业务功能完整性保持
